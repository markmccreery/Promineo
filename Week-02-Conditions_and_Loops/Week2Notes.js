//do-while loop will run the statement at least once.

//while loops need an increment ++ (add 1); otherwise it will create an infinite loop.
let i = 0;
while (i < 8) {
    console.log("before:" + " " + i);
    i = i + 1;
    console.log("after:" + " " + i);
}
//for
for (i = 0; i < 8; i++) {
    console.log(i)
}
//for in

//for of
//+= will add and then will reassign the result.  For example, clicks = clicks + strengthOfClicks will be the same thing as clicks += strengthOfClicks

//++i vs i++: i++ will print out the starting value before incrementing whereas the ++i will increment first then return i.


